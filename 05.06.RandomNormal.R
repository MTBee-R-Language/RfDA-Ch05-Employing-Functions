# To try online copy/paste to https://rdrr.io/snippets/
# 1 Create an R Script that assigns three random numbers to a variable and
#   displays the generated values
    nums <- rnorm( 3 )
    cat ( "Random Normal Distribution:"
	      , nums
	      , "\n\n"
	      )
# RandomNormal.R
# 2 Run the code several times to see the random
